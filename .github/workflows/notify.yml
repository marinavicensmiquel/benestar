name: Send Benestar notifications

on:
  schedule:
    - cron: "*/5 * * * *"   # every 5 minutes
  workflow_dispatch:        # allows manual trigger

jobs:
  send:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Firebase notification
        run: |
          curl -X POST "https://fcm.googleapis.com/v1/projects/benestarpwa/messages:send" \
            -H "Authorization: Bearer ${{ secrets.FCM_ACCESS_TOKEN }}" \
            -H "Content-Type: application/json; UTF-8" \
            -d '{
              "message": {
                "topic": "all",
                "notification": {
                  "title": "💚 Benestar",
                  "body": "It’s time for your wellbeing check"
                }
              }
            }'
